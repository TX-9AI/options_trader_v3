# TastyTrade official community SDK (OAuth, DXLink streamer, orders)
tastytrade>=12.4.0

# HTTP and async (required by tastytrade SDK)
httpx>=0.27.0
anyio>=4.0.0

# Market data: single shared TastyTrade/DXFeed candle store (data/candle_feed.py)
# — no external market-data dependency; sqlite3 is stdlib.

# Data / analysis
pandas>=2.0.0
numpy>=1.24.0

# Timezone handling
pytz>=2024.1

# SMS alerts (optional — gracefully disabled if not configured)
twilio>=8.0.0
