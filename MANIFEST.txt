HOTFIX_continuation_mid_nameerror_2026-07-29 — DEPLOY IMMEDIATELY

BREAKAGE: NameError: name 'mid' is not defined, thrown on EVERY TICK, killing the
main loop before any strategy evaluated. 15 boxes, zero trades from the open
through ~09:55 ET on 2026-07-29.

CAUSE: continuation_strategy v1.3 (the FVG pullback rewire, shipped 2026-07-28)
removed the BB-midline block that defined `mid`, but four references survived:
  line ~236  underlying_stop = mid - 0.5 * atr      <-- load-bearing
  line ~238  underlying_stop = mid + 0.5 * atr      <-- load-bearing
  line ~258  confluence string  f"...BB midline ({mid:.2f})..."
  line ~265  log line           f"midline={mid:.2f}..."

FIX (v1.3.1): the stop anchors to the FVG instead of the deleted midline, which
is what it should have been in the first place -- the gap IS the level the entry
was taken on.
  long  : entry tags gap.top from above; close through gap.bottom = breakdown
          -> underlying_stop = gap.bottom - 0.5*atr
  short : mirrors on gap.top -> underlying_stop = gap.top + 0.5*atr
`gap` is assigned at line 184, well before use at 239. Confluence and log lines
now report the gap range.

PROVEN: repaired block executed standalone --
  long tagged  -> stop 7427.5 (gap 7430-7433, atr 5)   long no-tag  -> None
  short tagged -> stop 7435.5                          short no-tag -> None
  orphaned 'mid' references remaining: 0

UNPACK + DEPLOY (from /home/ubuntu):
  cd ~/options-trader-v3 && tar -xzf ~/HOTFIX_continuation_mid_nameerror_2026-07-29.tar
  python -c "import py_compile;py_compile.compile('strategy/continuation_strategy.py',doraise=True);print('COMPILE OK')"
  git add strategy/continuation_strategy.py
  git commit -m "HOTFIX continuation v1.3.1: orphaned 'mid' refs after the FVG rewire threw NameError every tick, halting all trading"
  git push origin main
  -> then FULL wake+bake+restart+PYCACHE-CLEAR, immediately.

CONFIRM RECOVERY:
  cd ~/options-trader && journalctl -u optionsbot --since "5 min ago" | grep -c "name 'mid'"     # expect 0
  cd ~/options-trader && journalctl -u optionsbot --since "5 min ago" | grep -c "Tick #"          # expect > 0

SEPARATE, STILL OPEN -- do not confuse the two:
  "L2.5 unavailable (cannot import name 'RANGE_WINDOW_BARS' from
  analysis.conviction_integrator) - falling back to v1.3 classifier"
  Present on MU, NFLX, NVDA and likely fleet-wide. NOT caused by this hotfix and
  NOT fixed by it. The bots are running a DEGRADED regime engine (v1.3 classifier
  instead of the L2.5 integrator). Almost certainly fallout from the 2026-07-28
  confluence excavation. Needs its own diagnosis.

HOW THIS SHIPPED: v1.3 compiled and its FVG-tag geometry was unit-tested, but the
full generate_signal path was never EXECUTED -- the tastytrade SDK cannot be
installed in the sandbox, so only the extracted block was exercised. A compile
check cannot catch an unbound name on a branch that never ran. The canary needs
to import and CALL the strategy on a box before deploy, not just check symbols.
